{
  "name": "ezapply",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that fills repetitive Google Forms in one click. No build step — package.json exists only to run the tests.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  },
  "license": "MIT"
}
